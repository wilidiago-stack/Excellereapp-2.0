
{
  "entities": {
    "TimeSheetEntry": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "TimeSheetEntry",
      "type": "object",
      "description": "Represents a single entry of time logged by a user. Stored at top-level for global reporting.",
      "properties": {
        "userId": { "type": "string" },
        "projectId": { "type": "string" },
        "date": { "type": "string", "format": "date-time" },
        "hours": { "type": "number" },
        "description": { "type": "string" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["userId", "projectId", "date", "hours"]
    },
    "Project": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Project",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "companyName": { "type": "string" },
        "status": { "type": "string" }
      },
      "required": ["id", "name", "status"]
    },
    "UserProfile": {
      "title": "UserProfile",
      "type": "object",
      "properties": {
        "firstName": { "type": "string" },
        "lastName": { "type": "string" },
        "role": { "type": "string" },
        "assignedModules": { "type": "array", "items": { "type": "string" } }
      }
    },
    "Contractor": {
      "title": "Contractor",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "contactPerson": { "type": "string" },
        "status": { "type": "string", "enum": ["Active", "Inactive"] }
      },
      "required": ["name", "contactPerson", "status"]
    },
    "DailyReport": {
      "title": "DailyReport",
      "type": "object",
      "properties": {
        "projectId": { "type": "string" },
        "date": { "type": "string", "format": "date-time" },
        "shift": { "type": "string" },
        "authorId": { "type": "string" }
      }
    }
  },
  "auth": {
    "providers": ["password", "google.com", "microsoft.com"]
  },
  "firestore": {
    "structure": [
      {
        "path": "/time_entries/{entryId}",
        "definition": {
          "entityName": "TimeSheetEntry",
          "schema": { "$ref": "#/backend/entities/TimeSheetEntry" },
          "description": "Consolidated time entries for all users."
        }
      },
      {
        "path": "/projects/{projectId}",
        "definition": {
          "entityName": "Project",
          "schema": { "$ref": "#/backend/entities/Project" },
          "description": "Project data."
        }
      },
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "UserProfile",
          "schema": { "$ref": "#/backend/entities/UserProfile" },
          "description": "Extended user profile information."
        }
      },
      {
        "path": "/contractors/{contractorId}",
        "definition": {
          "entityName": "Contractor",
          "schema": { "$ref": "#/backend/entities/Contractor" },
          "description": "Registered contractors."
        }
      },
      {
        "path": "/dailyReports/{reportId}",
        "definition": {
          "entityName": "DailyReport",
          "schema": { "$ref": "#/backend/entities/DailyReport" },
          "description": "Daily site activities."
        }
      },
      {
        "path": "/monthlyReports/{reportId}",
        "definition": {
          "entityName": "MonthlyReport",
          "schema": { "type": "object" },
          "description": "Monthly site analysis."
        }
      }
    ]
  }
}
