rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: Insecure rules. For development only.
    // This is a temporary rule for debugging and opens up the entire database.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
