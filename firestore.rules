rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * EXCELLERE REVIVE - ROLE-BASED GRANULAR SECURITY
     */

    function isSignedIn() {
      return request.auth != null;
    }

    function getUserData() {
      let path = /databases/$(database)/documents/users/$(request.auth.uid);
      return exists(path) ? get(path).data : null;
    }

    function getRole() {
      let data = getUserData();
      return data != null ? data.get('role', 'viewer') : 'viewer';
    }

    function isAdmin() {
      return isSignedIn() && (
        exists(/databases/$(database)/documents/system_roles_admin/$(request.auth.uid)) ||
        getRole() == 'admin'
      );
    }

    function isProjectManager() {
      return isSignedIn() && getRole() == 'project_manager';
    }

    function hasModule(moduleId) {
      let data = getUserData();
      return isSignedIn() && data != null && data.get('assignedModules', []).hasAny([moduleId]);
    }

    /** 
     * USER PROFILES
     */
    match /users/{userId} {
      allow get: if isSignedIn();
      allow list: if isAdmin();
      allow create: if isAdmin();
      allow update: if isAdmin() || (
        isSignedIn() && 
        request.auth.uid == userId && 
        !request.resource.data.diff(resource.data).affectedKeys().hasAny(['role', 'assignedModules', 'status'])
      );
      allow delete: if isAdmin();
    }

    /** 
     * BUSINESS COLLECTIONS (PROJECTS, CONTRACTORS, REPORTS)
     */
    match /projects/{id} {
      allow read: if isSignedIn();
      allow create, update, delete: if isAdmin() || isProjectManager() || hasModule('projects');
    }

    match /contractors/{id} {
      allow read: if isSignedIn();
      allow create, update, delete: if isAdmin() || isProjectManager() || hasModule('contractors');
    }

    match /dailyReports/{id} {
      allow read: if isSignedIn();
      allow create, update, delete: if isAdmin() || isProjectManager() || hasModule('daily-report');
    }

    match /monthlyReports/{id} {
      allow read: if isSignedIn();
      allow create, update, delete: if isAdmin() || isProjectManager() || hasModule('monthly-report');
    }

    match /time_entries/{id} {
      // Allow read/list if admin OR owner
      allow read: if isSignedIn() && (isAdmin() || resource.data.userId == request.auth.uid);
      // Create requires accurate ownership mapping
      allow create: if isSignedIn() && request.resource.data.userId == request.auth.uid;
      // Update/Delete restricted to owner or admin
      allow update, delete: if isSignedIn() && (isAdmin() || resource.data.userId == request.auth.uid);
    }

    /** CONFIGURATION AND REFERENCES (Admin Only) */
    match /roles/{id} { allow read: if isSignedIn(); allow write: if isAdmin(); }
    match /modules/{id} { allow read: if isSignedIn(); allow write: if isAdmin(); }
    match /system_roles_admin/{adminUid} { allow read: if isSignedIn(); allow write: if isAdmin(); }
    match /system/metadata { allow read: if isSignedIn(); allow write: if isAdmin(); }
  }
}