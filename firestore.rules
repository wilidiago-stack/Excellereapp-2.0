rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Development rule: Allows any authenticated user to read and write.
    // This is secure enough for development, as it prevents anonymous access.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}